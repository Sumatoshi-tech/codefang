[package]
name = "cf-streaming"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

# Chunked / windowed aggregation over commit history with memory-bounded
# planning and analyzer hibernation. See specs/rust-rewrite/DESIGN.md.

[dependencies]
cf-alg = { path = "../cf-alg" }
cf-alg-stats = { path = "../cf-alg-stats" }
cf-units = { path = "../cf-units" }
cf-sigutil = { path = "../cf-sigutil" }
thiserror = "1"

[dev-dependencies]
cf-units = { path = "../cf-units" }
