suite: haskell
name: basic-functions

query_cases:
  - name: find_imports
    input: |
      import Data.List
    query: rfilter(.type == "Import") |> map(.type)
    output:
      results:
        - Import
