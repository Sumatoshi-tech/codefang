suite: imports
name: cpp-imports

query_cases:
  - name: import_structure
    input: |
      #include <iostream>
    query: 'rfilter(.type == "Import")'
    output:
      results:
        - type: Import
          roles: [Import]
          children:
            - type: Identifier
              roles: [Name]
              token: <iostream>
