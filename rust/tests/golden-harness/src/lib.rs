//! Empty library placeholder. The golden-diff harness lives in
//! `tests/golden_diff.rs`; this crate exists only to host that integration test
//! and to depend on the two binaries so Cargo builds them first.
