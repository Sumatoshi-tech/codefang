[package]
name = "cf-comments"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

# Port of internal/analyzers/comments — static comment density / quality
# (comment-block grouping sorted by line); also consumed by the quality
# analyzer. Report serialization routes through cf-gojson (the Go-byte-compatible
# encoder), never serde_json, per DESIGN §2.

[dependencies]
cf-gojson = { workspace = true }
cf-uast-node = { path = "../cf-uast-node" }
